#include "common_networking_socket_address.hpp"
#include <catch2/catch.hpp>

TEST_CASE("can use socket address", "[libcommon][networking][socket_address]") {
    /* auto sa = common::networking::socket_address{}; */
}
